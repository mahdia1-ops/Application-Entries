public class practiceproblems {
    int countOccurrences(String str, char ch) {
    int count = 0;
    for (int i = 0; i < str.length(); i++) {
        if (str.charAt(i) == ch) {
            count++;
        }}
    return count;
} 
//Problem number 1
    int elements = reverseArray.length;
    while (elements > 0){
            int max = reverseArray[0];
            for (int i = 1; i < reverseArray.length; i++) {
                if (reverseArray[i] > max) {
                    max = reverseArray[i];
                    System.out.println(max);
                    reverseArray = new int[elements - max];
                }
            }
    } 
//Problem number 2
public static double sumGrid(double[][] grid) {
    if (grid == null) return 0.0;
    double sum = 0.0;
    for (double[] row : grid) {
        if (row == null) continue;
        for (double v : row) {
            sum += v;
        }
    }
    return sum;
}
//problem number 3
       int numberOfTimes = 5;
        for (int i = 0; i < numberOfTimes; i++) {
            int n = 0;
            int b = 1;
            n += b;
        }
//problem number 4
}
